# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "contracts"
version = "0.4.0"
authors = ["karroffel <therzog@mail.de>"]
description = "Design-by-contract attributes"
documentation = "https://docs.rs/contracts"
readme = "README.md"
keywords = ["design-by-contract", "contract-programming", "precondition", "postcondition", "invariant"]
categories = ["development-tools", "development-tools::procedural-macro-helpers"]
license = "MPL-2.0"
repository = "https://gitlab.com/karroffel/contracts"

[lib]
name = "contracts"
path = "src/lib.rs"
proc-macro = true
[dependencies.proc-macro2]
version = "1.0"

[dependencies.quote]
version = "1.0"

[dependencies.syn]
version = "1.0"
features = ["extra-traits", "full", "visit-mut"]

[features]
disable_contracts = []
mirai_assertions = []
override_debug = []
override_log = []
[badges.gitlab]
branch = "master"
repository = "karroffel/contracts"
